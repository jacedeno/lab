apiVersion: v1
kind: ConfigMap
metadata:
  name: pr-app-config
  namespace: purchase-requisition
  labels:
    app.kubernetes.io/name: purchase-requisition
data:
  FLASK_ENV: "production"
  # SMTP (uncomment when ready)
  # SMTP_HOST: "smtp.example.com"
  # SMTP_PORT: "587"
  # NOTE: BUYER_EMAILS is now managed via app/config/buyers.yaml (no env var needed)
