# CloudNativePG Operator v1.28.1
#
# Install with:
#   kubectl apply --server-side -f https://raw.githubusercontent.com/cloudnative-pg/cloudnative-pg/release-1.28/releases/cnpg-1.28.1.yaml
#
# Verify with:
#   kubectl get deployment -n cnpg-system
#   kubectl get pods -n cnpg-system
#   kubectl get crd | grep cnpg
#
# This file is a reference — apply directly from the upstream URL above.
# Keeping a local copy is optional but useful for air-gapped environments.
